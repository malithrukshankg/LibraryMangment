1.Front end is inside the client_front_end folder
2.Back end is inside the node folder
3.MySql data base has been used
4.data base variables and the port of the back end can be changed using in .env file(Node/.env)
5.url of the http methods in front end gard coded to port 3000. Because of that use the port of back end as 3000
6.when login use name=Jack
		 password=abcdef
7.when login as admin use name=admin
                          password=admin
8.when serchin the user first name should be given as inputs following names are used as hardcoded values
      
    		i.Jack
		ii.drashi  
		iii.malith     
                iv.chamodi
                v.chamika    
